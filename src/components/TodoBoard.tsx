import { Stack } from "@mui/material";
import { FilterButton } from "./FilterButton.tsx";

export const TodoBoard = () => {
    return (
        <Stack>
            <FilterButton />
        </Stack>
    );
};
